const CartTotal = () => {
  return (
    <div className="cart__total">
      <div className="cart__total-value">Total: $</div>
      <button className="cart__checkout-button">Proceed to checkout</button>
    </div>
  );
};

export { CartTotal };
